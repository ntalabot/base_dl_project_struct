# `src` package

## Main components
* `data`: datasets, data loading, etc.
* `model`: model definition
* `train`: train a model, define the loss, optimizer
* `test`: evaluate a model, and make predictions
* `utils`: misc. utilities like visualization, generic functions, etc.